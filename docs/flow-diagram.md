# PawGo 完整页面流程图

上门宠物美容匹配平台 MVP · 三端页面流转（客户 / 美容师 / 后台）

## 一、总览：三端页面地图

```mermaid
flowchart TD
    Start([启动 App]) --> Auth[登录/注册页<br/>手机号OTP + 角色选择]

    Auth -->|我是客户| C1[首页]
    Auth -->|我是美容师| G1[接单大厅]

    %% ============ 客户端 ============
    subgraph CLIENT[客户端 · 宠物主人]
        C1 --> C2[发布需求页]
        C2 --> C3[匹配结果页<br/>推荐 1-3 位美容师]
        C3 --> C4[支付定金页<br/>PayNow/信用卡]
        C4 --> C5[订单详情页<br/>状态进度条 + 进入聊天]
        C5 -->|匹配成功+定金| C6[聊天页<br/>隐私临时聊天]
        C5 --> C7[服务完成与评价页]
        C7 --> C1
        C1 --> C8[我的页面]
        C8 --> C1
    end

    %% ============ 美容师端 ============
    subgraph GROOMER[美容师端]
        G1 -->|开启接单| G2[附近可接订单列表]
        G2 --> G3[订单详情页·未接单]
        G3 -->|接单| G4[进行中订单页]
        G4 -->|我已出发| G4
        G4 -->|我已到达| G4
        G4 -->|标记服务完成| G5[聊天页<br/>同隐私规则]
        G5 --> G6[收入与历史页]
        G1 --> G7[个人资料与设置页]
        G7 --> G1
    end

    %% ============ 后台 ============
    subgraph ADMIN[后台 · 极简管理]
        A1[美容师审核] --> A2[订单总览]
        A2 --> A3[聊天记录查看<br/>仅纠纷]
        A3 --> A4[基础数据看板]
    end

    %% 跨端连接
    C3 -.匹配推荐.-> G2
    C4 -.定金支付.-> G4
    C6 <-.聊天通道.-> G5
    C7 -.完成+评价.-> G4
```

## 二、客户端主闭环（时序/泳道）

```mermaid
sequenceDiagram
    participant O as 客户
    participant S as 系统
    participant G as 美容师
    participant P as 支付/聊天

    O->>S: 发布上门美容需求<br/>(宠物/体型/服务/时间窗/地址/预算)
    S->>S: 匹配引擎硬过滤+排序权重
    S-->>O: 推荐 1-3 位美容师
    O->>S: 选择一位确认
    S->>P: 进入支付定金
    O->>P: 支付定金 (PayNow)
    P-->>S: 订单=已确认, 聊天开启
    S->>G: 推送新订单通知
    G->>S: 接单/出发/到达/服务中
    O->>P: 进入临时聊天 (文字/图片)
    G->>P: 沟通服务细节
    G->>S: 标记服务完成
    S-->>P: 聊天自动关闭 (隐私)
    S-->>O: 弹出评价页
    O->>S: 星级+文字评价
    S-->>G: 评价计入档案
```

## 三、订单状态机 × 页面映射

| 状态 | 触发动作 | 客户端可见页 | 美容师可见页 | 聊天 |
|------|----------|--------------|--------------|------|
| `matching` | 提交需求 | 匹配结果页 | 接单大厅(列表中) | 关闭 |
| `awaiting_deposit` | 客户选美容师 | 支付定金页 | (待接单) | 关闭 |
| `confirmed` | 定金支付 | 订单详情+聊天入口 | 进行中订单 | **开启** |
| `in_progress` | 美容师出发/到达 | 订单详情+聊天 | 进行中(状态操作) | **开启** |
| `completed` | 标记完成 | 评价页 | 收入历史 | 自动关闭 |
| `cancelled` | 取消规则 | 我的/历史 | 进行中 | 关闭 |

## 四、关键设计原则（来自计划书）

- **大按钮、少步骤、一眼能懂**：每页单一主行动
- **极简**：MVP 只做匹配闭环，不做遛狗/陪伴
- **隐私优先**：聊天仅在「匹配成功+定金」后开启，完成后自动关闭，不暴露真实手机号/完整地址
- **双向选择**：系统推荐 3 个最优 + 客户自主选择
